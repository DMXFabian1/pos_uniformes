# Copia este archivo como `pos_uniformes.env` junto al ejecutable o en la raiz del proyecto.
# La app lo leera automaticamente cuando no existan variables de entorno del sistema.

# IP de la PC servidor (donde corre Postgres). Unico lugar donde vive esta IP:
# el codigo la lee de aqui, nunca la trae codificada.
POS_UNIFORMES_SERVER_HOST=192.168.0.10
POS_UNIFORMES_DB_HOST=192.168.0.10
POS_UNIFORMES_DB_PORT=5432
POS_UNIFORMES_DB_NAME=pos_uniformes
POS_UNIFORMES_DB_USER=postgres
POS_UNIFORMES_DB_PASSWORD=1234
POS_UNIFORMES_DB_ECHO=0
POS_UNIFORMES_AUTO_CREATE_SCHEMA=0
# Opcional: segunda copia del respaldo automatico en carpeta sincronizada o disco externo.
POS_UNIFORMES_BACKUP_EXTERNAL_DIR=
# Opcional: techo por query en ms (default 30000). Evita que una query colgada
# congele la app; 0 = sin limite.
# POS_UNIFORMES_DB_STATEMENT_TIMEOUT_MS=30000

# Opcional: DVR de cámaras (visor en el kiosko, Ctrl+Shift+C). Si no se
# configura aquí, se captura en Administración del satélite → Cámaras.
# POS_UNIFORMES_DVR_HOST=192.168.0.11
# POS_UNIFORMES_DVR_USER=dany
# POS_UNIFORMES_DVR_PASSWORD=

# Opcional: resumen diario del negocio por Telegram (scripts\instalar_resumen_diario.bat).
# Token del bot (lo da @BotFather) y tu chat id (se descubre con
# scripts\resumen_diario_telegram.bat --chat-ids después de escribirle al bot).
# POS_UNIFORMES_TELEGRAM_BOT_TOKEN=
# POS_UNIFORMES_TELEGRAM_CHAT_ID=
